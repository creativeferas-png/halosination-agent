# HALO production dependencies
# Pinned to exact versions verified in the build venv.
# No langchain/langgraph - HALO uses a custom Python orchestrator (see ARCHITECTURE.md §2.2).

fastapi==0.115.0
uvicorn==0.32.0
openai==1.54.0
python-dotenv==1.0.1
pydantic==2.9.2
httpx==0.27.2
